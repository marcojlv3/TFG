import { StyleSheet } from 'react-native';

const eColor = '#128F56';
const mColor = '#0777C6';
const dColor = '#E80D3F';

export const styles = StyleSheet.create({

    listContainer: {
        height:  124,
        margin: 5,
        borderRadius: 5,
        borderWidth: 2 
    },
    eBorder: {
        borderColor: eColor,
    },
    mBorder:{
        borderColor: mColor,
    },
    dBorder: {
        borderColor: dColor,
    }


});